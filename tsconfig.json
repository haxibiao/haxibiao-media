{
    "compilerOptions": {
        "target": "esnext",
        "module": "esnext",
        // this enables stricter inference for data properties on `this`
        "strict": false,
        "noImplicitAny": false,
        "jsx": "preserve",
        "moduleResolution": "node"
    }
}
