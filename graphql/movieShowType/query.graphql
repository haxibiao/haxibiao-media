type MovieShowType {
  id: ID
  name: String
  movies: [Movie]
}

extend type Query {
  movieShowTypeList: MovieShowType
    @paginate(
      defaultCount: 10
      builder: "Haxibiao\\Media\\MovieShowType@movieShowTypeList"
    )
}
