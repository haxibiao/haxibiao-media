extend type Mutation {
    """
    上传图片
    """
    uploadImage(
        """
        图片(base64)
        """
        image: [String]
    ): [String] @field(resolver: "App\\Image@resolveUploadImage")
}
